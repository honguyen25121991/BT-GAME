import {Text, StyleSheet, View} from 'react-native';
import React, {Component} from 'react';
import {BackgroundView} from '../../components';

export default class MapingScree1n extends Component {
  render() {
    return (
      <BackgroundView>
        <Text>index</Text>
      </BackgroundView>
    );
  }
}
